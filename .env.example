# ============================================================================
# Environment Variables Template
# ============================================================================
# Copy this file to .env and fill in your actual values
# IMPORTANT: Never commit .env to version control!
# ============================================================================

# Google Cloud Platform Configuration
# Required: Your GCP Project ID
GCP_PROJECT_ID=your-gcp-project-id
REGION=your-region
BACKUP_BUCKET=gs://your-backup-bucket

# Optional: Path to service account JSON key file
# If not set, uses default application credentials (gcloud auth application-default login)
GOOGLE_APPLICATION_CREDENTIALS=/path/to/your/service-account-key.json

# Kaggle API Credentials (for dataset download)
# ----------------------------------------------------------------------------
# Get your credentials from: https://www.kaggle.com/account
KAGGLE_USERNAME=your-kaggle-username
KAGGLE_KEY=your-kaggle-api-key

# Environment Configuration
# Set to "dev", "staging", or "prod"
ENVIRONMENT=dev

# BigQuery Configuration
# Database (BigQuery Project)
BQ_DATABASE=your-gcp-project-id

# BigQuery Dataset Names (Schemas)
# ----------------------------------------------------------------------------
# IMPORTANT: These should match your dbt target configuration!
#
# In development (ENVIRONMENT=dev):
#   - Raw data is loaded to: staging (by Python ingestion)
#   - dbt sources read from: staging
#   - dbt creates models in: dev_warehouse_staging, dev_warehouse_warehouse
#
# In production (ENVIRONMENT=prod):
#   - Raw data is loaded to: staging (by Python ingestion)
#   - dbt sources read from: staging
#   - dbt creates models in: staging, warehouse
# ----------------------------------------------------------------------------

# Raw data landing zone (where Kaggle data is loaded)
BQ_DATASET_RAW=staging

# Staging models dataset (dbt staging layer)
# Dev: dev_warehouse_staging | Prod: staging
BQ_DATASET_STAGING=dev_warehouse_staging

# Warehouse models dataset (dbt warehouse layer - facts, dimensions, marts)
# Dev: dev_warehouse_warehouse | Prod: warehouse
BQ_DATASET_WAREHOUSE=dev_warehouse_warehouse

# Logging
# Optional: Log level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# ============================================================================
# Setup Instructions:
# 1. Copy this file: cp .env.example .env
# 2. Fill in your actual credentials in .env
# 3. Verify .env is in .gitignore (it should be)
# 4. Never commit .env to version control
# ============================================================================
